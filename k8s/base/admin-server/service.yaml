apiVersion: v1
kind: Service
metadata:
  name: admin-server
  namespace: petclinic-dev
spec:
  type: ClusterIP
  selector:
    app: admin-server
  ports:
    - port: 9090
      targetPort: 9090
      protocol: TCP
